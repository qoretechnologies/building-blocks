#!/usr/bin/env qore
#
# Qorus Integration Engine * workflow test

%new-style
%require-types
%enable-all-warnings

%exec-class MainTest

%requires QorusInterfaceTest
%requires Util
%requires ssh2

%include ../libraries/uk-lib-test-common.qfd

class MainTest inherits QorusWorkflowTest {
    constructor() : QorusWorkflowTest ("SftpWorkflowModuleTest", "1.0", \ARGV) {
        addTestCase("test all steps", \test_all steps());
        set_return_value(main());
    }

    test_1() {
        # qrest.put("jobs/" + JOB_NAME + "?action=run");
        exec(new WaitForWfiid(wfiid));
    }
}
