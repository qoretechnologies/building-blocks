#!/usr/bin/env qore
# -*- mode: qore; indent-tabs-mode: nil -*-
# author: Qore Technologies

%new-style
%require-types
%strict-args
%enable-all-warnings

%requires QUnit

%requires QMock
%requires QorusMock

%append-module-path ../../
%requires SftpModule

%exec-class Main

class Main inherits QUnit::Test {
    constructor() : QUnit::Test("SftpModuleTest", "1.0") {
        # test cases
        addTestCase("test 1", \test_1());

        set_return_value(main());
    }

    setUp() {
        QorusMock::refresh();
        QorusMock::setDefaultMocks();
    }

    test_1() {
        # TODO
    }
}
